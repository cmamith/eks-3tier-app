name: DevSecOps Pipeline (Step 1 - Setup)

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  setup:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Verify Environment
        run: |
          echo "✅ GitHub Actions environment ready!"
          pwd
          ls -la
    
  sonar-scan:
    name: SonarQube Scan
    runs-on: self-hosted
    needs: setup

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java (required by SonarQube)
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install SonarScanner (macOS ARM)
        run: |
          echo "🍺 Installing SonarScanner ARM version..."
          arch -arm64 brew update
          arch -arm64 brew install sonar-scanner || echo "Already installed"
          arch -arm64 sonar-scanner --version 

      - name: Run SonarQube Analysis (Local)
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: "http://localhost:9000"
        run: |
          echo "🔍 Running SonarQube analysis on local instance..."
          sonar-scanner \
            -Dsonar.projectKey=eks-3tier-app \
            -Dsonar.projectName=eks-3tier-app \
            -Dsonar.sources=. \
            -Dsonar.sourceEncoding=UTF-8 \
            -Dsonar.host.url=$SONAR_HOST_URL \
            -Dsonar.login=$SONAR_TOKEN

      - name: Wait for SonarQube Quality Gate Result
        env:
          SONAR_HOST_URL: "http://localhost:9000"
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          echo "⏳ Checking Quality Gate status..."
          for i in {1..20}; do
            status=$(curl -s -u $SONAR_TOKEN: "$SONAR_HOST_URL/api/qualitygates/project_status?projectKey=eks-3tier-app" | jq -r '.projectStatus.status')
            echo "Current status: $status"
            if [ "$status" == "OK" ]; then
              echo "✅ Quality Gate PASSED!"
              exit 0
            elif [ "$status" == "ERROR" ]; then
              echo "❌ Quality Gate FAILED!"
              exit 1
            else
              echo "⏳ Waiting for analysis to finish..."
              sleep 15
            fi
          done
          echo "⚠️ Timed out waiting for Quality Gate result."
          exit 1


# -----------------------------
# 3️⃣ Trivy IaC Config Scan (Cloud)
# -----------------------------
  trivy-config-scan:
    runs-on: self-hosted

    steps:
      - name: 🧱 Checkout repository
        uses: actions/checkout@v4

      - name: 🔍 Run Trivy Config Scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: config
          format: table
          output: trivy-config-report.txt
          exit-code: 0
          severity: HIGH,CRITICAL

      - name: 📤 Upload Config Scan Report
        uses: actions/upload-artifact@v4
        with:
          name: trivy-config-report
          path: trivy-config-report.txt


  # -----------------------------
  # 4️⃣ Trivy Docker Image Scan (Local)
  # -----------------------------
  trivy-image-scan:
    runs-on: self-hosted
    needs: trivy-config-scan

    steps:
      - name: 🧱 Checkout repository
        uses: actions/checkout@v4

      - name: 🐳 Build Docker image
        run: |
          cd apps/backend/auth
          docker build -t auth-service:latest .

      - name: 🔍 Run Trivy Image Scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: auth-service:latest
          format: table
          output: trivy-image-report.txt
          exit-code: 0
          severity: HIGH,CRITICAL

      - name: 📤 Upload Image Scan Report
        uses: actions/upload-artifact@v4
        with:
          name: trivy-image-report
          path: trivy-image-report.txt

  # -----------------------------
  # 6️⃣ OWASP ZAP Dynamic Scan (DAST)
  # -----------------------------
  zap-scan:
    runs-on: self-hosted
    needs: trivy-image-scan
    steps:
      - name: 🧱 Checkout repository
        uses: actions/checkout@v4

      - name: 🌐 Start Application Container (for scanning)
        run: |
          docker run -d -p 8080:8080 --name authentication-service auth-service:latest
          sleep 10
          echo "App running on http://localhost:8080"

      - name: 🔒 Run OWASP ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.11.0
        with:
          target: "http://localhost:8080"
          cmd_options: "-a -T 10"   # '-a' = active scan, '-T 10' = timeout 10 mins
        continue-on-error: true

      - name: 📤 Upload ZAP Report
        uses: actions/upload-artifact@v4
        with:
          name: owasp-zap-report
          path: report_html.html

      - name: 🧹 Cleanup Containers
        if: always()
        run: docker rm -f auth-service || true



  # -----------------------------
  # 5️⃣ Summary Stage
  # -----------------------------
  # summary:
  #   runs-on: ubuntu-latest
  #   needs: trivy-image-scan
  #   steps:
  #     - name: ✅ Summary
  #       run: echo "✨ All scans completed successfully — SonarQube, Trivy Config, and Trivy Image."
 